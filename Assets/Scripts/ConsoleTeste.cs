using UnityEngine;

public class ConsoleTeste : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Ola Mundo");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
